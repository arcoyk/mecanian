<%= form_for(@page) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>
      <ul>
      <% @page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  Title:
  <%= f.text_field :title, :class => "form form-field" %>
  Content (Markdown compatible):
  <%= f.text_area :content, :class => "form form-area" %>
  <%= f.number_field :security %>
  <br>
  <%= f.submit %>

<% end %>
